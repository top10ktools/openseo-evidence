import {readFile,readdir} from 'node:fs/promises';const files=(await readdir('dist')).filter(x=>/\.(js|html|css)$/.test(x));const bad=[];const sinks=/\b(fetch|XMLHttpRequest|WebSocket|EventSource|sendBeacon|RTCPeerConnection)\b|import\s*\(\s*[^'"`]/;for(const f of files){const s=await readFile(`dist/${f}`,'utf8');if(/(?:https?:)?\/\//i.test(s)||sinks.test(s))bad.push(f);if(f==='index.html'&&!s.includes("connect-src 'none'"))bad.push('missing CSP');}if(bad.length)throw new Error(`Privacy check failed: ${bad.join(', ')}`);console.log('Privacy static check passed: no outbound URL/API patterns; CSP present.');
